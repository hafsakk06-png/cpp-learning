#include <iostream>
using namespace std;

    // we can makr it in double,sum,and float//i use sum.
    //sum of 2 numbers
    int sum(int a, int b) {
        int s = a+b;
        return s;
    }

    int main() {
        cout <<sum (10,5) << endl;

        cout << (10+5);
        return 0;
    }

